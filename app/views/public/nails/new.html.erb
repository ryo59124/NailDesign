<h1>New Nail DESIGN</h1>
  <%= form_with model: @nail do |f| %>
    <h4>Image</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Body</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Post!!' %>
  <% end %>